const initialState = {
  page3: {},
};

function page3Reducer(state = initialState) {
  if (typeof state === 'undefined') {
    return initialState;
  }
  return state;
}

export default page3Reducer;
