const initialState = {
  page2component3: {},
};

function page2Component3Reducer(state = initialState) {
  if (typeof state === 'undefined') {
    return initialState;
  }
  return state;
}

export default page2Component3Reducer;
