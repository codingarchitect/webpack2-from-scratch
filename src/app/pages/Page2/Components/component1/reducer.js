const initialState = {
  page2component1: {},
};

function page2Component1Reducer(state = initialState) {
  if (typeof state === 'undefined') {
    return initialState;
  }
  return state;
}

export default page2Component1Reducer;
