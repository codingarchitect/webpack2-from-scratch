const initialState = {
  page1component2: {},
};

function page1Component2Reducer(state = initialState) {
  if (typeof state === 'undefined') {
    return initialState;
  }
  return state;
}

export default page1Component2Reducer;
